<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Test</title>
</head>
<body>
    <h1>Test</h1>
    {{ range $index, $person := . }}
        <p>{{ $person.picture }}</p>
        <p>{{ $person.name }}</p>
        <p>{{ $person.gender }}</p>
        <p>{{ $person.age }}</p>
        <hr>
    {{ end }}
</body>
</html>
